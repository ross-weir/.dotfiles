- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.i3: i3
    ~/.config/termite: termite
    ~/.gitconfig: gitconfig
    ~/.zshrc: zshrc
    ~/.aliases: aliases

- shell:
  - [git submodule update --init --recursive, Installing submodules]
