- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.dotfiles: ''
    ~/.gitconfig: gitconfig
    ~/.zshrc: zshrc
    ~/.aliases: aliases

- shell:
  - [git submodule update --init --recursive, Installing submodules]
